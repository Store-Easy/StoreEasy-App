import React from 'react';
import { Button, View } from 'react-native';

const UserType = () => {
    return(
        <View>
            <Button/>
            <Button/>
        </View>
    )
}

export default UserType;